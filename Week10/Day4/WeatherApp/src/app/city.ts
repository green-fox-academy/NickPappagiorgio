export class City {
  city: string;
  location: string;
  weather: [
    { temp: number, icon: string, message: string },
    { temp: number, icon: string, message: string },
    { temp: number, icon: string, message: string },
    { temp: number, icon: string, message: string },
    { temp: number, icon: string, message: string }
  ]
}
